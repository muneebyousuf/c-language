#include <stdio.h>
int main() {
    int units;
    float bill;
    printf("Enter electricity units used: ");
    scanf("%d", &units);

    if (units <= 100)
        bill = units * 5;
    else
        bill = (100 * 5) + (units - 100) * 7;

    printf("Your Bill = ₹%.2f\n", bill);
    return 0;
}
